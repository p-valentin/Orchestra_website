{
  "mcpServers": {
    "orchestra-license-test": {
      "command": "sh",
      "args": [
        "-c",
        "cd supabase && PATH=\"$HOME/.deno/bin:$PATH\" exec deno run -A mcp/server.ts"
      ]
    }
  }
}
